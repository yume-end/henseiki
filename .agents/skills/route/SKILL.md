---
name: route
description: Scaffold routes, build CRUD features for existing models, or add auth protection to existing routes.
---

- **Scaffold a single route or layout** — read `.agents/skills/route/page.md`
- **Build CRUD routes for an existing model** — read `.agents/skills/route/crud.md` (table, validators, and model must already exist — use `/model` first if not)
- **Add auth to an existing route** — read `.agents/skills/route/protect.md`
